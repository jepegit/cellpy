package:
  name: cellpy
  version: "0.2.0"

source:
  git_rev: release/v.0.2.0
  git_url: https://github.com/jepegit/cellpy

requirements:
  host:
    - python
    - setuptools

  run:
    - python

test:
  imports:
    -

about:
  home:
  license:
  license_file:
