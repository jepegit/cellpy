version: 2

build:
    image: latest

sphinx:
    configuration: docs/conf.py

conda:
    environment: docs/environment_rtd.yml

python:
    version: 3.8
    install:
        - method: setuptools
        - path: .

