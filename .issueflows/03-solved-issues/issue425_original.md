# Issue #425: Iterative fixes: get() docstring units

Source: https://github.com/jepegit/cellpy/issues/425

## Original issue text

Interactive `/iflow-fix` session.

Individual fixes are recorded in the issue status markdown and landed together via `/iflow-close`.

First fix: document `cellpy_units` defaults for `nominal_capacity` and `area` in `cellpy.get()` docstring.
