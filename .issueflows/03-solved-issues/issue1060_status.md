# Issue #1060 status

- [x] Done

## What's done

- `dev` group: `cellpy-connectors @ git+https://github.com/cellpy/cellpy-connectors.git` (lock pin `4979e70`).
- `tests/test_cli_connectors_plugin.py`: help without import, `connectors ping`, `import cellpy` isolation. Essential. Skip if dist missing.
- CONTRIBUTING git-pin note; test-registry rows; HISTORY.

## Remaining work

- None.
