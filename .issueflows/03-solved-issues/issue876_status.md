# Issue #876 — Status

- [x] Done

## What's done

- Removed Tier-1 path filters so real checks also run for docs-only changes.
- Removed the same-named docs-only dummy workflow.
- Preserved required `essential (linux / uv)` and `full (linux / uv)` names.
- Updated adjacent workflow comments, CI guidance, and `HISTORY.md`.
- Changed workflows pass `actionlint`; focused trigger/name checks pass.

## Remaining work

- None.
