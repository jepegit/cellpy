# Issue #983 status

- [ ] Done

## What's done

- Plan accepted.
- `legend_replacer` takes the two integer ids from 2- or 3-part Plotly names
  (`group, direction, subgroup` as px.line actually emits).
- Three-part names: cell label for `name` / `legendgroup` /
  `legendgrouptitle_text`.
- Unit tests + `per_cycle` / `direction="both"` collected plot test
  (essential).
- Note in `plotting-collected.md`.

## Remaining work

- HISTORY bullet + close.
