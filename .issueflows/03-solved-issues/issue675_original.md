# Issue #675: Iterative fixes: pin-cellpycore

Source: https://github.com/jepegit/cellpy/issues/675

## Original issue text

Interactive `/iflow-fix` session (landing via `/iflow-close yolo`).

Individual fixes are recorded in the status markdown and landed together in one PR.

**First fix:** pin `cellpycore` from `0.2.3` â†’ `0.2.4` in `pyproject.toml` / `uv.lock` (new cellpy-core release on PyPI).
