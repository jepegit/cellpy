# Issue #1089 status

- [x] Done

## What's done

- Plan accepted: (a) prefix-checked remote load.
- `find_cells(kind=raw)` calls `find_by_project`; remote URIs listed;
  `needs_metadata` always on raw.
- `load_cell` allows configured remote URIs only (no `..`, no invented host).
- Tests in cellpy-mcp. Docs + HISTORY.

## Remaining work

None.
