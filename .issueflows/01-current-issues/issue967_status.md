# Issue #967 — status

- [ ] Done

## What's done

- Picked `#967` (`iflow pick 967`). Branch
  `cursor/967-zensical-docs-markers-8d63`.
- Captured original; comments empty (section omitted).
- Plan drafted, then revised: rewrite source docstrings instead of a
  Griffe hook. Waiting on Accept / Revise / Abort.

## Remaining work

- Confirm revised plan, then rewrite Sphinx roles in `cellpy/**/*.py`,
  add a `dev_docs.md` note and a recurrence grep test.
- Verify a clean `zensical build` with no leftover `:class:` / `:meth:` in `site/`.
