# Issue #938 status

- [ ] Done

## What's done

- Captured original + comment triage.
- Yolo scope check aborted: three independent deliverables (mdb-export error, discovery API, examplesdir default).

## Remaining work

- Split or run the normal `/iflow-plan` → `/iflow-build` path.
- Part 1: named error when `mdb-export` is missing.
- Part 2: `list_instruments` reports unavailable loaders with a reason.
- Part 3 (comment): absolute default `examplesdir` (consider folding into #960).
